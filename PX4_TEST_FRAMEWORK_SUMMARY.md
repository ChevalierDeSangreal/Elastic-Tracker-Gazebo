# PX4 测试框架 - 项目总结

## 📋 项目概述

已成功创建了一个完整的 PX4+Gazebo 测试框架，用于对比 **Elastic-Tracker** 和 **神经网络控制** 两种目标跟踪方法。

**项目位置**：`/home/core/wangzimo/Elastic-Tracker/src/px4_test_framework/`

## 🏗️ 系统架构

```
┌──────────────────────────────────────────────────────────────┐
│                  PX4 SITL + Gazebo 仿真                       │
│            (提供真实的飞行动力学和环境)                        │
└────────────────────┬─────────────────────────────────────────┘
                     │
                     ↓
┌──────────────────────────────────────────────────────────────┐
│                      MAVROS                                   │
│         (MAVLink ↔ ROS 通信桥梁)                             │
│  • /mavros/local_position/odom                               │
│  • /mavros/setpoint_raw/attitude                             │
│  • /mavros/imu/data                                          │
└────┬───────────────────────────────────────────────────┬─────┘
     │                                                   │
     ↓                                                   ↓
┌─────────────────────────┐              ┌─────────────────────────┐
│  Elastic-Tracker 方案    │              │  神经网络控制方案        │
├─────────────────────────┤              ├─────────────────────────┤
│                         │              │                         │
│ 1️⃣ 目标发布器           │              │ 1️⃣ 目标发布器           │
│    ↓                    │              │    ↓                    │
│ 2️⃣ Elastic规划器        │              │ 2️⃣ 神经网络节点         │
│    (轨迹规划)           │              │    • 观测状态            │
│    ↓                    │              │    • 神经网络推理        │
│ 3️⃣ 轨迹服务器           │              │    • 生成控制指令        │
│    (轨迹插值)           │              │    ↓                    │
│    ↓                    │              │ 3️⃣ PX4姿态控制          │
│ 4️⃣ SO3控制器            │              │                         │
│    (姿态控制)           │              │                         │
│    ↓                    │              │                         │
│ 5️⃣ SO3→PX4转换器        │              │                         │
│    (指令转换)           │              │                         │
└─────────────────────────┘              └─────────────────────────┘
           ↓                                        ↓
┌──────────────────────────────────────────────────────────────┐
│              性能评估工具 (performance_evaluator.py)          │
│  • 记录轨迹                                                   │
│  • 计算误差指标                                               │
│  • 生成对比图表                                               │
└──────────────────────────────────────────────────────────────┘
```

## 📦 核心组件

### 1. SO3 到 PX4 转换器 (`so3_to_px4_node`)

**功能**：将 Elastic-Tracker 的 SO3 控制指令转换为 PX4 姿态控制指令

**输入**：
- `so3cmd` (quadrotor_msgs/SO3Command): 推力向量和期望姿态
- `/mavros/local_position/odom`: 当前状态

**输出**：
- `/mavros/setpoint_raw/attitude`: PX4 姿态控制指令

**关键代码**：
```cpp
// 文件：src/so3_to_px4_node.cpp
// 推力归一化
double normalized_thrust = force.norm() / max_thrust_;
// 姿态设置
attitude_target.orientation = desired_orientation;
```

### 2. 目标发布器 (`target_publisher_node`)

**功能**：发布目标的位置和速度，支持多种运动模式

**支持的模式**：
- **静止模式** (mode=0): 固定位置
- **圆周模式** (mode=1): 圆形轨迹，带加速/减速
- **8字模式** (mode=2): 待实现

**输出**：
- `/target/odom`: 目标里程计（兼容 Elastic-Tracker）
- `/target/position`: 目标位置
- `/target/velocity`: 目标速度

**关键参数**：
```yaml
circle_radius: 2.0      # 圆半径 [m]
circle_duration: 20.0   # 单圈时间 [s]
stationary_time: 3.0    # 初始静止时间 [s]
```

### 3. 神经网络跟踪节点 (`neural_tracking_node`)

**功能**：使用神经网络策略进行目标跟踪

**当前实现**：PD 控制器占位符（需要替换为实际的神经网络）

**接口设计**：
```cpp
// 状态向量：[相对位置(3), 相对速度(3), 姿态(4), 角速度(3)]
Eigen::VectorXd state(13);

// 动作向量：[推力(1), 姿态四元数(4), 角速度(3)]
Eigen::VectorXd action(8);
```

**如何集成您的神经网络**：
1. 在构造函数中加载模型（TensorFlow Lite, ONNX等）
2. 修改 `inference()` 函数实现推理
3. 确保状态和动作向量格式匹配

### 4. 性能评估工具 (`performance_evaluator.py`)

**功能**：记录轨迹数据并计算性能指标

**输出指标**：
- 平均跟踪误差
- 误差标准差
- 最大误差
- RMSE（均方根误差）
- 平均/最大速度

**输出文件**：
- `*_metrics.txt`: 文本格式的指标
- `*_trajectory.npz`: NumPy 格式的轨迹数据
- `*_plot.png`: 轨迹和误差可视化图

## 📂 项目结构

```
px4_test_framework/
├── CMakeLists.txt              # 编译配置
├── package.xml                 # ROS 包配置
├── README.md                   # 完整文档
├── QUICKSTART.md              # 快速开始指南
│
├── config/                     # 配置文件
│   ├── so3_to_px4_params.yaml       # SO3 转换器参数
│   ├── target_params.yaml            # 目标轨迹参数
│   ├── elastic_planner_params.yaml   # Elastic 规划器参数
│   └── px4_test.rviz                 # RViz 配置
│
├── include/px4_test_framework/ # 头文件
│   ├── so3_to_px4_node.hpp
│   ├── target_publisher_node.hpp
│   └── neural_tracking_node.hpp
│
├── src/                        # 源文件
│   ├── so3_to_px4_node.cpp
│   ├── so3_to_px4_main.cpp
│   ├── target_publisher_node.cpp
│   ├── target_publisher_main.cpp
│   ├── neural_tracking_node.cpp
│   └── neural_tracking_main.cpp
│
├── launch/                     # 启动文件
│   ├── px4_simulation.launch         # 基础仿真环境
│   ├── elastic_tracking.launch       # Elastic 跟踪测试
│   └── neural_tracking.launch        # 神经网络跟踪测试
│
└── scripts/                    # 脚本工具
    ├── run_elastic_test.sh           # Elastic 测试脚本
    ├── run_neural_test.sh            # 神经网络测试脚本
    ├── performance_evaluator.py      # 性能评估工具
    └── setup_environment.sh          # 环境设置脚本
```

## 🚀 使用流程

### 1. 编译

```bash
cd /home/core/wangzimo/Elastic-Tracker
catkin_make
source devel/setup.bash
```

### 2. 启动仿真环境

**终端 1**：PX4 SITL
```bash
cd ~/PX4-Autopilot
make px4_sitl gazebo
```

**终端 2**：MAVROS + 目标发布器
```bash
roslaunch px4_test_framework px4_simulation.launch
```

### 3. 控制方法

Elastic Tracker
```bash
roslaunch px4_test_framework elastic_tracking.launch
```
### 4. 性能评估

```bash
rosrun px4_test_framework performance_evaluator.py _test_name:=test1
```

## 📊 测试建议

### 测试矩阵

| 测试 | 控制方法 | 目标类型 | 半径 | 速度 | 说明 |
|------|---------|---------|------|------|------|
| 1 | Elastic | 静止 | - | - | 基准测试 |
| 2 | Neural | 静止 | - | - | 基准测试 |
| 3 | Elastic | 圆周 | 2m | 慢 | 低速跟踪 |
| 4 | Neural | 圆周 | 2m | 慢 | 低速跟踪 |
| 5 | Elastic | 圆周 | 2m | 快 | 高速跟踪 |
| 6 | Neural | 圆周 | 2m | 快 | 高速跟踪 |

### 评估指标

**主要指标**：
- 平均跟踪误差 (越小越好)
- RMSE (越小越好)
- 最大误差 (越小越好)

**辅助指标**：
- 平均速度 (反映控制器活跃度)
- 误差标准差 (反映稳定性)

## 🔧 参数调优

### SO3 控制器

文件：`src/uav_simulator/config/so3_controller.yaml`

```yaml
gains:
  rot:  {x: 1.5, y: 1.5, z: 1.0}  # 姿态增益
  ang:  {x: 0.13, y: 0.13, z: 0.1} # 角速度增益
```

### Elastic 规划器

文件：`config/elastic_planner_params.yaml`

```yaml
vmax: 1.5    # 最大速度 [m/s]
amax: 2.5    # 最大加速度 [m/s^2]
tracking_dist: 3.0  # 期望跟踪距离 [m]
```

### 目标轨迹

文件：`config/target_params.yaml`

```yaml
circle_radius: 2.0       # 调整轨迹大小
circle_duration: 20.0    # 调整运动速度
```

## 🎯 对比实验步骤

1. **准备**
   - 确保环境稳定
   - 配置相同的目标轨迹参数
   - 准备性能评估工具

2. **测试 Elastic**
   ```bash
   roslaunch px4_test_framework elastic_tracking.launch
   rosrun px4_test_framework performance_evaluator.py _test_name:=elastic_r2_slow
   # 等待 30 秒
   # Ctrl+C 停止
   ```

3. **重启环境**
   ```bash
   # 停止所有节点
   # 重新启动 PX4 和 MAVROS
   ```

4. **测试神经网络**
   ```bash
   roslaunch px4_test_framework neural_tracking.launch
   rosrun px4_test_framework performance_evaluator.py _test_name:=neural_r2_slow
   # 等待 30 秒
   # Ctrl+C 停止
   ```

5. **对比分析**
   ```bash
   cat /tmp/tracking_results/elastic_*_metrics.txt
   cat /tmp/tracking_results/neural_*_metrics.txt
   ```

## 📝 下一步工作

### 1. 集成您的神经网络模型

**位置**：`src/neural_tracking_node.cpp`

**需要修改的函数**：
- 构造函数：加载模型
- `inference()`：神经网络推理
- `build_state_vector()`：可能需要调整状态格式

**参考代码**：
```cpp
// 在构造函数中
#include <tensorflow/lite/interpreter.h>
model_ = tflite::FlatBufferModel::BuildFromFile(model_path_.c_str());

// 在 inference() 中
float* input = interpreter_->typed_input_tensor<float>(0);
memcpy(input, state.data(), state.size() * sizeof(float));
interpreter_->Invoke();
float* output = interpreter_->typed_output_tensor<float>(0);
```

### 2. 添加更多目标轨迹

在 `target_publisher_node.cpp` 中实现：
- 8字轨迹
- 螺旋轨迹
- 随机轨迹

### 3. 增强性能评估

在 `performance_evaluator.py` 中添加：
- 更多性能指标（控制能耗、执行器使用率等）
- 实时性能监控界面
- 多次实验的统计分析

### 4. 添加障碍物环境

集成 Elastic-Tracker 的障碍物检测和规划功能。

## ❓ 常见问题

### 编译问题

**Q**: 找不到 `quadrotor_msgs`  
**A**: 确保在 Elastic-Tracker 根目录编译：
```bash
cd /home/core/wangzimo/Elastic-Tracker
catkin_make
```

### 运行问题

**Q**: MAVROS 无法连接  
**A**: 
1. 确认 PX4 正在运行：`pgrep -f px4`
2. 检查端口：`netstat -an | grep 14540`

**Q**: 无人机不响应  
**A**: 
1. 检查飞行模式：`rostopic echo /mavros/state`
2. 确认控制频率 > 2Hz

### 神经网络问题

**Q**: 如何加载我的模型？  
**A**: 修改 `neural_tracking_node.cpp`，参考上面的代码示例。

**Q**: 状态向量格式是什么？  
**A**: 当前是 13 维：`[相对位置(3), 相对速度(3), 姿态(4), 角速度(3)]`，可根据需要调整。

## 📚 参考资料

- [Elastic-Tracker 论文](https://arxiv.org/abs/2109.07111)
- [PX4 开发指南](https://docs.px4.io/)
- [MAVROS 文档](http://wiki.ros.org/mavros)
- [ROS Noetic 教程](http://wiki.ros.org/noetic)

## 🎉 总结

您现在拥有一个完整的测试框架，可以：

✅ 在 PX4+Gazebo 中测试 Elastic-Tracker  
✅ 集成和测试您的神经网络控制器  
✅ 自动记录和对比性能指标  
✅ 灵活配置目标轨迹和控制参数  

祝您实验顺利！如有问题，请参考文档或提交 Issue。

