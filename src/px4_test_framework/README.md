# PX4 测试框架

基于 PX4+Gazebo 的测试框架，用于测试 **Elastic-Tracker** 目标跟踪方法。

**完全集成 RealFlight_ros 状态机框架**，通过状态机进行位置初始化，无需手动操作 PX4。

## 📚 文档导航

- **[命名空间设计](NAMESPACE_DESIGN.md)** - 详细的命名空间架构设计
- **[命名空间使用指南](docs/NAMESPACE_GUIDE_CN.md)** - 命名空间使用说明和调试技巧
- **[命名空间总结](docs/NAMESPACE_SUMMARY.md)** - 话题/服务映射表和数据流图

## 📋 目录

- [项目概述](#项目概述)
- [系统架构](#系统架构)
- [安装依赖](#安装依赖)
- [编译项目](#编译项目)
- [使用方法](#使用方法)
- [参数配置](#参数配置)
- [常见问题](#常见问题)

## 项目概述

本测试框架将 Elastic-Tracker 的规划器和 SO3 控制器与 PX4+Gazebo 仿真环境集成：

1. **Elastic-Tracker 方法**：使用弹性轨迹规划器进行目标跟踪
2. **状态机集成**：完全兼容 RealFlight_ros 的状态机框架，自动执行起飞、悬停、跟踪流程
3. **性能评估**：自动记录轨迹和计算性能指标

**注意**：神经网络控制测试在 RealFlight_ros 中独立进行，本框架专注于 Elastic Tracker。

## 系统架构

**重要**：本框架**不重新实现**控制方法，而是直接调用 Elastic-Tracker 原有实现，并完全集成 RealFlight_ros 的状态机框架。

```
┌─────────────────────────────────────────────────────────────┐
│                    PX4 SITL + Gazebo                        │
│                   (无人机仿真环境)                           │
└─────────────────────┬───────────────────────────────────────┘
                      │
                      ↓
┌─────────────────────────────────────────────────────────────┐
│                      MAVROS                                 │
│              (ROS ↔ PX4 通信桥梁)                           │
└─────────────────────┬───────────────────────────────────────┘
                      │
                      ↓
┌─────────────────────────────────────────────────────────────┐
│              状态机框架 (RealFlight_ros)                     │
│  INIT → ARMING → TAKEOFF → HOVER → TRAJ → END_TRAJ → LAND  │
└─────────────────────┬───────────────────────────────────────┘
                      │
                      ↓ (进入 TRAJ 状态时自动触发)
┌─────────────────────────────────────────────────────────────┐
│              Elastic Tracker 跟踪系统                        │
├─────────────────────────────────────────────────────────────┤
│ 1. 状态监听器                                               │
│    - 监听状态机状态                                         │
│    - 进入 TRAJ 时自动触发 Elastic Tracker                    │
│    ↓                                                        │
│ 2. 目标发布器                                               │
│    → /target/odom (nav_msgs/Odometry)                       │
│    ↓                                                        │
│ 3. Elastic规划器 (原有实现)                                 │
│    ↓                                                        │
│ 4. 轨迹服务器 (原有实现)                                    │
│    ↓                                                        │
│ 5. SO3控制器 (原有实现)                                      │
│    ↓                                                        │
│ 6. SO3→PX4转换器 (新增，适配PX4)                            │
│    → /mavros/setpoint_raw/attitude                         │
└─────────────────────────────────────────────────────────────┘
```

### 核心设计理念

1. **复用原有实现**：不重新实现控制算法，直接调用 Elastic-Tracker 和 RealFlight_ros 的原有节点
2. **状态机集成**：完全集成 RealFlight_ros 状态机框架，实现自动化初始化流程
3. **最小化修改**：只添加必要的转换器（SO3→PX4）和状态监听器，其他保持原样

### 核心组件

1. **state_listener_node**: 状态监听器
   - 监听 RealFlight_ros 状态机的状态变化
   - 当进入 TRAJ 状态时，自动发送触发信号给 Elastic Tracker
   - 实现与状态机框架的完全兼容

2. **target_publisher_node**: 目标发布器
   - 发布 `nav_msgs/Odometry` 格式的目标（Elastic-Tracker 使用）
   - 支持圆周、静止等轨迹模式
   
3. **so3_to_px4_node**: SO3→PX4 转换器
   - 将 Elastic-Tracker 的 SO3 控制指令转换为 PX4 姿态控制指令
   - 这是唯一新增的转换节点

4. **performance_evaluator.py**: 性能评估工具
   - 记录轨迹数据
   - 计算性能指标
   - 生成可视化图表

**注意**：控制方法本身使用原有实现：
- **Elastic Tracker**: 直接使用 `planning` 和 `so3_controller` 节点
- **状态机**: 直接使用 RealFlight_ros 的 `offboard_state_machine` 节点

## 安装依赖

### 1. ROS环境

```bash
# Ubuntu 20.04 + ROS Noetic
sudo apt install ros-noetic-desktop-full
```

### 2. PX4 + Gazebo

```bash
cd ~
git clone https://github.com/PX4/PX4-Autopilot.git --recursive
cd PX4-Autopilot
bash ./Tools/setup/ubuntu.sh
make px4_sitl gazebo
```

### 3. MAVROS

```bash
sudo apt install ros-noetic-mavros ros-noetic-mavros-extras
sudo apt install ros-noetic-tf2*

# 安装 GeographicLib 数据集
wget https://raw.githubusercontent.com/mavlink/mavros/master/mavros/scripts/install_geographiclib_datasets.sh
chmod +x install_geographiclib_datasets.sh
sudo ./install_geographiclib_datasets.sh
```

### 4. 其他依赖

```bash
sudo apt install libeigen3-dev
sudo apt install python3-matplotlib python3-numpy
```

## 编译项目

```bash
cd /home/core/wangzimo/Elastic-Tracker
catkin_make
source devel/setup.bash
```

## 使用方法

### 方式一：分步启动（推荐用于调试）

**重要**：必须按顺序启动，因为 `elastic_tracking.launch` 依赖 `px4_simulation.launch` 提供的 MAVROS 连接和目标发布器。

#### 步骤 1: 启动 PX4 SITL + Gazebo

```bash
# 终端 1
cd ~/PX4-Autopilot
make px4_sitl gazebo
```

**等待 PX4 完全启动**（看到 "Ready to fly" 等信息后再进行下一步）。

#### 步骤 2: 启动基础仿真环境（必须先启动）

```bash
# 终端 2
cd /home/core/wangzimo/Elastic-Tracker
source devel/setup.bash
roslaunch px4_test_framework px4_simulation.launch
```

这会启动：
- **MAVROS**（ROS ↔ PX4 通信桥梁）- **必需**
- **目标发布器**（发布目标轨迹到 `/target/odom`）- **必需**
- **RViz 可视化**（可选，默认启用）

**验证连接**：等待几秒后，检查 MAVROS 是否连接成功：
```bash
rostopic echo /drone0/mavros/state -n 1
```
应该看到 `connected: True`。

**目标发布器配置**（可选）：
- 默认通过 `px4_simulation.launch` 自动启动，使用 `config/target_params.yaml` 配置
- 单独启动：`rosrun px4_test_framework target_publisher_node`
- 修改参数：编辑 `config/target_params.yaml` 或使用命令行参数：
  ```bash
  rosrun px4_test_framework target_publisher_node \
    _mode:=1 \
    _circle_radius:=3.0 \
    _circle_center_z:=2.0 \
    _stationary_time:=5.0
  ```
- 主要参数：`mode` (0=静止, 1=圆周), `circle_radius`, `circle_center_z`, `circle_duration`, `stationary_time`

#### 步骤 3: 启动 Elastic Tracker 跟踪系统

```bash
# 终端 3（确保步骤 2 已经启动并连接成功）
cd /home/core/wangzimo/Elastic-Tracker
source devel/setup.bash
roslaunch px4_test_framework elastic_tracking.launch
```

这会启动：
- **状态机**（`offboard_state_machine`）：自动执行 ARMING → TAKEOFF → HOVER
- **状态监听器**：监听状态机状态，在 TRAJ 状态时触发 Elastic Tracker
- **Elastic Tracker 规划器**：轨迹规划节点
- **轨迹服务器**：将规划轨迹转换为位置指令
- **SO3 控制器**：将位置指令转换为 SO3 控制指令
- **SO3→PX4 转换器**：将 SO3 指令转换为 PX4 姿态控制指令
- **可视化节点**：无人机和目标的可视化 Marker

**注意**：此 launch 文件默认不启动 RViz（避免与步骤 2 冲突），如果需要单独启动可以添加 `use_rviz:=true`。

**完整工作流程**：
1. **PX4 SITL 启动**：在 Gazebo 中显示无人机
2. **MAVROS 连接**：建立 ROS 与 PX4 的通信
3. **目标发布器启动**：开始发布目标位置（初始静止位置）
4. **状态机自动执行**：INIT → ARMING → TAKEOFF → HOVER
5. **无人机自动起飞**：到达悬停高度（默认 1.2m）
6. **进入跟踪模式**：手动触发或等待进入 TRAJ 状态
7. **目标开始运动**：进入 TRAJ 状态后，目标静止 3 秒，然后开始圆周运动
8. **Elastic Tracker 自动跟踪**：状态监听器检测到 TRAJ 状态后，自动触发 Elastic Tracker 开始跟踪

**手动触发 TRAJ 状态**（可选）：

```bash
# 终端 4（可选）
rostopic pub /state/command_drone_0 std_msgs/Int32 "data: 5" --once
```

状态值说明：
- `0`: INIT
- `1`: ARMING
- `2`: TAKEOFF
- `3`: GOTO
- `4`: HOVER
- `5`: TRAJ ← 触发 Elastic Tracker
- `6`: END_TRAJ
- `7`: LAND
- `8`: DONE

#### 步骤 4: 启动性能评估（可选）

```bash
# 终端 4（或新终端）
rosrun px4_test_framework performance_evaluator.py _test_name:=elastic_test
# 结果保存在 /tmp/tracking_results/
```

## 参数配置

### 1. SO3 到 PX4 转换器参数

文件：`config/so3_to_px4_params.yaml`

```yaml
mass: 1.5              # 无人机质量 [kg]
g: 9.81                # 重力加速度 [m/s^2]
max_thrust: 25.0       # 最大推力 [N]
use_body_rate: false   # 是否使用角速率控制
```

### 2. 目标轨迹参数

文件：`config/target_params.yaml`

```yaml
mode: 1                # 0:静止, 1:圆周, 2:8字
circle_radius: 2.0     # 圆半径 [m]
circle_center_x: 0.0   # 圆心X坐标 [m]
circle_center_y: 0.0   # 圆心Y坐标 [m]
circle_center_z: 1.5   # 圆心高度 [m]
circle_duration: 20.0  # 单圈时间 [s]
stationary_time: 3.0   # 进入TRAJ状态后的静止等待时间 [s]
ramp_up_time: 3.0      # 加速时间 [s]
ramp_down_time: 3.0    # 减速时间 [s]
```

**说明**：
- 目标发布器自动监听状态机状态，进入 TRAJ 状态前保持初始位置
- 进入 TRAJ 状态后等待 `stationary_time` 秒，然后开始运动
- 发布话题：`/target/odom` (Elastic-Tracker使用), `/target/position`, `/target/velocity`

### 3. Elastic Tracker 参数

文件：`config/elastic_planner_params.yaml`

```yaml
vmax: 1.5              # 最大速度 [m/s]
amax: 2.5              # 最大加速度 [m/s^2]
tracking_dist: 3.0     # 跟踪距离 [m]
```
